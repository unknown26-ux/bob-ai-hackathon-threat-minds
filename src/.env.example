# Copy this file to .env and fill in your actual values
# NEVER commit the .env file — it is in .gitignore

# ThreatLens backend
APP_PORT=8000
APP_ENV=development

# SQLite database path (relative to src/backend/)
# Default: threatlens.db (created automatically on startup)
DATABASE_PATH=threatlens.db
